{% extends 'base.html.twig' %}

{% block title %}Edit Habit{% endblock %}

{% block body %}

    <main role="main">
        <div class="jumbotron">
            <div class="container"> <h1>Edit this habit or delete it</h1></div>
        </div>

        <div class="container">
            <nav class="navbar-dark  bg-dark">
                <div class="input-group" >
                    <div class="input-group-append">
                        <img src="{{ asset('../img/icon_calendar.png') }}" alt="logo">
                    </div>
                </div>
            </nav>
            <div class="jumbotron">
                <div class="container">
                    {{ include('habit/_form.html.twig', {'button_label': 'Update'}) }}
                    {{ include('habit/_delete_form.html.twig') }}
                </div>
                <a class="btn btn-secondary" href="{{ path('app_habit_index') }}">back to list</a>
            </div>
        </div>
    </main>

{#    <h1>Edit Habit</h1>#}

{#    {{ include('habit/_form.html.twig', {'button_label': 'Update'}) }}#}

{#    <a href="{{ path('app_habit_index') }}">back to list</a>#}

{#    {{ include('habit/_delete_form.html.twig') }}#}
{% endblock %}
