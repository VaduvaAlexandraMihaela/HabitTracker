<form method="post" action="{{ path('app_rec_habit_delete', {'id': rec_habit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rec_habit.id) }}">
    <button class="btn">Delete</button>
</form>
