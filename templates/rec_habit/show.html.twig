{% extends 'base.html.twig' %}

{% block title %}RecHabit{% endblock %}

{% block body %}
    <h1>RecHabit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rec_habit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rec_habit.Name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ rec_habit.Duration }}</td>
            </tr>
            <tr>
                <th>TimeStart</th>
                <td>{{ rec_habit.TimeStart ? rec_habit.TimeStart|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rec_habit_index') }}">back to list</a>

    <a href="{{ path('app_rec_habit_edit', {'id': rec_habit.id}) }}">edit</a>

    {{ include('rec_habit/_delete_form.html.twig') }}
{% endblock %}
