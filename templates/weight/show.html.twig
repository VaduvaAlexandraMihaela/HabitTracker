{% extends 'base.html.twig' %}

{% block title %}Weight{% endblock %}

{% block body %}
    <h1>Weight</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weight.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ weight.Value }}</td>
            </tr>
            <tr>
                <th>WeightLost</th>
                <td>{{ weight.WeightLost }}</td>
            </tr>
            <tr>
                <th>Goal</th>
                <td>{{ weight.Goal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_weight_index') }}">back to list</a>

    <a href="{{ path('app_weight_edit', {'id': weight.id}) }}">edit</a>

    {{ include('weight/_delete_form.html.twig') }}
{% endblock %}
